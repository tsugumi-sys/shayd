# SuzumeDB

A pure SQLite-compatible database engine written in Rust.

SuzumeDB aims to reimplement SQLite from the ground up in safe Rust while preserving compatibility with the SQLite file format and SQL semantics.

The long-term goal is to explore how modern Rust and operating system APIs can improve the safety, maintainability, and performance of embedded database engines.
