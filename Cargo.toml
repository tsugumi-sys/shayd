[package]
name = "oxlite"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
license = "MIT"
description = "A pure SQLite-compatible database engine written in Rust."

[lib]
name = "oxlite"
path = "src/lib.rs"

[dependencies]
